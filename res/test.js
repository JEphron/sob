function helloWorld(blah) {
    return blah + 1;
}

module.exports.FOO = helloWorld('bar');

/*
comment starts here
still going - - the question is does it break if we go too long?
and we're done
*/

zzz = ["this line is very long", "it's actually gonna be longer", "let's keep", "going", 1, 2, 3, 4, 5, 65];
zzz = [
        "let's try something",
        "where we wrap around"
      ];
